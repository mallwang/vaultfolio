# domain-accounts

Framework-independent domain shape and validation for the Account Overview feature
(`specs/025-account-overview`) — mirrors `libs/domain/holdings`'s pattern (Principle I).

## Running unit tests

Run `npm exec nx test domain-accounts` to execute the unit tests via Jest.
