describe('market-data', () => {
  it('remains an empty, reserved placeholder (TODO(MARKET_DATA_PROVIDER))', () => {
    // No provider is selected or wired yet — see README.md and the constitution's
    // open TODO(MARKET_DATA_PROVIDER). This test only proves the library builds
    // and tests in isolation, per FR-007 / Principle I.
    expect(true).toBe(true);
  });
});
