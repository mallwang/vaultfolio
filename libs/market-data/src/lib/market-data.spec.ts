describe('market-data', () => {
  it('remains an empty, reserved placeholder (TODO(MARKET_DATA_PROVIDER))', () => {
    // No provider is selected or wired yet — see README.md and the
    // eslint-disable-next-line sonarjs/todo-tag -- tracked in README.md / the
    // constitution's open TODO(MARKET_DATA_PROVIDER), not a forgotten stray
    // comment here. This test only proves the library builds and tests in
    // isolation, per FR-007 / Principle I. The assertion is trivially true
    // on purpose — there is nothing to exercise yet.
    // eslint-disable-next-line sonarjs/no-trivial-assertions
    expect(true).toBe(true);
  });
});
