meta {
  name: Session
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/auth/session
  body: none
  auth: none
}

docs {
  Requires the `vaultfolio_session` cookie captured by `Login` — run that
  request first.
}
