# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.nx/self-healing
.angular

# Handlebars notification templates/partials (libs/notifications) — Prettier's
# bundled hbs parser doesn't support standalone (non-component) partials
# like `{{> header-en}}`.
*.hbs
